name: flutter_application_2
description: Smart Expense Splitter for Groups
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase - Using latest stable versions
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.3
  cloud_firestore: ^5.6.1

  # PDF Generation
  pdf: ^3.11.1
  
  # File & Path utilities
  path_provider: ^2.1.5
  
  # Sharing
  share_plus: ^10.1.3
  
  # Date formatting
  intl: ^0.19.0

  # UI
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true